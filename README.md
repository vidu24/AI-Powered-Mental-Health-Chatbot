# AI-Powered-Mental-Health-Chatbot
set up your virtual environment , for than you can refer any online sources
after setting up the environment install rasa and so on ... as it avoids conflicts with other projects "pip install rasa"
Initialize a new Rasa project" rasa init --no-prompt"
